import "./scss/styles.scss"

/**
 * Organize content in common `JSON` structure
 * In a real application this would be our payload
 * from an API call
 */
const data = {
  positive: "Positive content",
  neutral: "Neutral content",
  negative: "Negative content",
}

const modifiers = {
  activeClass: "btn--active",
}

const attributes = {
  value: "data-value",
}
